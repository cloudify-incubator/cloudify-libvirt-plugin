<domain xmlns:qemu="http://libvirt.org/schemas/domain/qemu/1.0" xmlns="" type="qemu">
  <resource>
    <partition>/machine</partition>
  </resource>
  <uuid>{{ instance_uuid }}</uuid>
  <currentMemory unit="KiB">{{ memmory_minsize }}</currentMemory>
  <on_poweroff>destroy</on_poweroff>
  <devices>
    <console type="pty" tty="/dev/pts/1">
      <alias name="serial0"/>
      <target type="serial" port="0"/>
      <source path="/dev/pts/1"/>
    </console>
    <memballoon model="virtio">
      <alias name="balloon0"/>
      <address function="0x0" type="pci" domain="0x0000" slot="0x04" bus="0x00"/>
    </memballoon>
    <controller type="usb" index="0">
      <alias name="usb0"/>
      <address function="0x2" type="pci" domain="0x0000" slot="0x01" bus="0x00"/>
    </controller>
    <controller type="pci" index="0" model="pci-root">
      <alias name="pci.0"/>
    </controller>
    <controller type="ide" index="0">
      <alias name="ide0"/>
      <address function="0x1" type="pci" domain="0x0000" slot="0x01" bus="0x00"/>
    </controller>
    <video>
      <alias name="video0"/>
      <model heads="1" type="cirrus" vram="9216"/>
      <address function="0x0" type="pci" domain="0x0000" slot="0x02" bus="0x00"/>
    </video>
    <emulator>/usr/bin/qemu-system-x86_64</emulator>
    <graphics autoport="yes" type="vnc" listen="127.0.0.1" port="5900">
      <listen type="address" address="127.0.0.1"/>
    </graphics>
    {% for network in networks %}
      <interface type="network">
        <target dev="{{ network.dev }}"/>
        <alias name="{{ network.name }}"/>
        <source network="{{ network.network }}"/>
        <mac address="{{ network.mac }}"/>
        <address function="{{ network.address.function }}"
                 type="{{ network.address.type }}"
                 domain="{{ network.address.domain }}"
                 slot="{{ network.address.slot }}"
                 bus="{{ network.address.bus }}"/>
        <model type="{{ network.type }}"/>
      </interface>
    {% endfor %}
    <input type="mouse" bus="ps2"/>
    <input type="keyboard" bus="ps2"/>
    <serial type="pty">
      <alias name="serial0"/>
      <target port="0"/>
      <source path="/dev/pts/1"/>
    </serial>
    {% for disk in disks %}
      <disk device="disk" type="file">
        <target dev="{{ disk.dev }}" bus="{{ disk.bus }}"/>
        <source file="{{ disk.file }}"/>
        <driver type="{{ disk.type }}" name="qemu"/>
        <alias  name="{{ disk.name }}"/>
        <address type="drive"
                 target="{{ disk.address.target }}"
                 unit="{{ disk.address.unit }}"
                 bus="{{ disk.address.bus }}"
                 controller="{{ disk.address.controller }}"/>
      </disk>
    {% endfor %}
  </devices>
  <on_crash>restart</on_crash>
  <on_reboot>restart</on_reboot>
  <vcpu placement="static">1</vcpu>
  <features>
    <pae/>
    <acpi/>
    <apic/>
  </features>
  <memory unit="KiB">{{ memmory_size }}</memory>
  <os>
    <type machine="pc" arch="x86_64">hvm</type>
    <boot dev="hd"/>
  </os>
  <clock offset="utc"/>
  <name>{{ resource_id }}</name>
</domain>
